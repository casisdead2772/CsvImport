<nav class="navbar navbar-dark dark navbar-expand-lg navbar-static-top">
    <div class="container">
        <a class="navbar-brand" href="{{ path('upload_file') }}">
            <img src="{{ asset('images/import-csv.png') }}" class="logo"/>
        </a>
        <button
                class="navbar-toggler"
                type="button"
                data-bs-toggle="collapse"
                data-bs-target="#navbarSupportedContent"
                aria-controls="navbarSupportedContent"
                aria-expanded="false">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav mr-auto px-2">
                <li class="nav-item active">
                    <a class="nav-link" href="{{ path('upload_file') }}">
                        <h5>Home</h5>
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" aria-current="page" href="{{ path('imports') }}">
                        <h5>Import History</h5>
                    </a>
                </li>
            </ul>
        </div>
    </div>
</nav>